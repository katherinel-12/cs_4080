class Toast {}
var breakfast = Toast();

print breakfast.butter;

// test for Ch. 30 Challenge 1
fun fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2);
}

var start = clock();
print fib(35);
print clock() - start;
